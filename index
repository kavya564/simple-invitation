<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>You're Invited!</title>
  <style>
    body {
      margin: 0;
      padding: 0;
      background: linear-gradient(to bottom right, #ffafbd, #ffc3a0);
      font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
      color: #fff;
      text-align: center;
      overflow: hidden;
    }

    .container {
      padding-top: 60px;
    }

    h1 {
      font-size: 3em;
      margin-bottom: 20px;
      animation: pop 1s ease-in-out;
    }

    #sentence {
      font-size: 1.8em;
      margin-top: 30px;
      padding: 0 20px;
      animation: fadeIn 2s ease-in-out;
    }

    .celebration-img {
      width: 250px;
      height: auto;
      margin-top: 30px;
      animation: bounce 2s infinite;
    }

    /* Animations */
    @keyframes pop {
      0% { transform: scale(0.5); opacity: 0; }
      100% { transform: scale(1); opacity: 1; }
    }

    @keyframes fadeIn {
      from { opacity: 0; }
      to { opacity: 1; }
    }

    @keyframes bounce {
      0%, 100% { transform: translateY(0); }
      50% { transform: translateY(-20px); }
    }

    /* Party burst */
    .confetti {
      position: fixed;
      top: 0;
      left: 0;
      width: 100%;
      height: 100%;
      pointer-events: none;
      background-image: url('https://media.giphy.com/media/3oEjI6SIIHBdRxXI40/giphy.gif'); /* party burst gif */
      background-repeat: no-repeat;
      background-position: center;
      background-size: cover;
      opacity: 0.6;
      animation: fadeIn 2s ease-in-out;
    }
  </style>
</head>
<body>

  <div class="confetti"></div>

  <div class="container">
    <h1>🎉 You're Invited! 🎊</h1>
    
    <img class="celebration-img" src="https://cdn-icons-png.flaticon.com/512/3163/3163738.png" alt="Party Image">
    
    <div id="sentence">Loading your invitation...</div>
  </div>

  <script>
    const sentences = [
      "🎈 Welcome to the celebration of the year!",
      "🎊 Let’s make memories that last forever!",
      "💥 Mee kosam oka special surprise undi.",
      "🌟 You’re the sparkle we need at this party!",
      "🕺 Party time! Get ready to groove.",
      "🎆 Celebration starts with you!",
      "✨ Let’s light up the night together.",
      "👗 Dress up and show up – this is your moment!",
      "🎁 Ee roju mee kosame.",
      "🎉 Get ready for unlimited fun and magic!"
    ];

    // Randomly pick one
    const randomIndex = Math.floor(Math.random() * sentences.length);
    document.getElementById("sentence").innerText = sentences[randomIndex];
  </script>

</body>
</html>
